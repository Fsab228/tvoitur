# Инструкция по запуску сайта локально

Эта инструкция поможет вам запустить сайт на вашем компьютере для разработки и тестирования.

## Требования

Перед началом убедитесь, что у вас установлены:

1. **Node.js** (версия 18 или выше)
   - Скачать: https://nodejs.org/
   - Проверить установку: `node --version`
   - Должна быть установлена версия 18.x.x или выше

2. **npm** (устанавливается вместе с Node.js)
   - Проверить установку: `npm --version`

## Шаг 1: Установка зависимостей

Откройте PowerShell или командную строку в папке проекта и выполните:

```powershell
cd C:\Users\seraf\tvoitur
npm install
```

Эта команда установит все необходимые библиотеки и зависимости проекта. Это может занять 1-3 минуты.

**Что происходит:** npm читает файл `package.json` и устанавливает все зависимости, указанные в нем (React, TypeScript, Vite, Tailwind CSS и т.д.).

## Шаг 2: Запуск сайта в режиме разработки

После установки зависимостей запустите сайт:

```powershell
npm run dev
```

Вы увидите сообщение примерно такого вида:
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## Шаг 3: Открытие сайта в браузере

1. Скопируйте адрес из консоли (обычно `http://localhost:5173/`)
2. Откройте браузер (Chrome, Firefox, Edge и т.д.)
3. Вставьте адрес в адресную строку и нажмите Enter

Сайт откроется и будет автоматически обновляться при изменении файлов!

## Что вы увидите

- **Главная страница** с популярными турами и горящими предложениями
- **Каталог туров** (`/tours`) - все 24 тура с фильтрацией и поиском
- **Горящие туры** (`/hot-tours`) - туры со скидкой
- **Акции** (`/promotions`) - специальные предложения
- **О компании** (`/about`) - информация о компании
- **Контакты** (`/contacts`) - контактная информация

## Полезные команды

### Запуск в режиме разработки
```powershell
npm run dev
```
- Запускает локальный сервер разработки
- Автоматически перезагружает страницу при изменении файлов
- Показывает ошибки в консоли браузера

### Сборка для продакшена
```powershell
npm run build
```
- Создает оптимизированную версию сайта в папке `dist/`
- Используется перед публикацией на GitHub Pages
- Файлы готовы для размещения на сервере

### Предпросмотр собранной версии
```powershell
npm run preview
```
- Запускает локальный сервер для просмотра собранной версии
- Полезно для проверки, как сайт будет выглядеть после публикации

### Проверка кода на ошибки
```powershell
npm run lint
```
- Проверяет код на наличие ошибок и предупреждений
- Помогает поддерживать качество кода

## Остановка сервера

Чтобы остановить локальный сервер разработки:
1. Вернитесь в окно PowerShell/командной строки
2. Нажмите `Ctrl + C`
3. Подтвердите остановку, нажав `Y` или `Enter`

## Решение проблем

### Ошибка "npm не является внутренней или внешней командой"
- **Решение:** Node.js не установлен или не добавлен в PATH
- Переустановите Node.js с официального сайта: https://nodejs.org/
- При установке убедитесь, что выбрана опция "Add to PATH"

### Ошибка "EACCES: permission denied"
- **Решение:** Проблема с правами доступа
- Запустите PowerShell от имени администратора
- Или используйте `npm install --force`

### Порт 5173 уже занят
- **Решение:** Другой процесс использует этот порт
- Остановите другой процесс, использующий порт 5173
- Или Vite автоматически предложит использовать другой порт

### Сайт не открывается
- Проверьте, что сервер запущен (в консоли должно быть сообщение "ready")
- Убедитесь, что используете правильный адрес (обычно `http://localhost:5173/`)
- Попробуйте очистить кэш браузера (`Ctrl + Shift + Delete`)

### Изображения не загружаются
- Убедитесь, что папка `public/images/` существует
- Проверьте консоль браузера на ошибки загрузки
- Изображения загружаются из интернета (Unsplash/Pexels), нужен интернет

### Изменения не отображаются
- Сохраните файл (`Ctrl + S`)
- Vite должен автоматически перезагрузить страницу
- Если не помогло, остановите сервер (`Ctrl + C`) и запустите снова (`npm run dev`)

## Структура проекта

```
tvoitur/
├── src/                    # Исходный код
│   ├── components/         # Компоненты (Header, Footer, TourCard и т.д.)
│   ├── pages/             # Страницы сайта
│   ├── data/              # Данные о турах
│   ├── types/             # TypeScript типы
│   └── utils/             # Вспомогательные функции
├── public/                # Публичные файлы (изображения)
├── index.html             # Главный HTML файл
├── package.json           # Зависимости проекта
├── vite.config.ts         # Конфигурация Vite
└── tailwind.config.js     # Конфигурация Tailwind CSS
```

## Редактирование туров

Чтобы изменить туры, откройте файл:
```
src/data/tours.ts
```

В этом файле находятся все 24 тура. Вы можете:
- Изменить цены
- Добавить новые туры
- Изменить описания
- Обновить изображения

После сохранения файла сайт автоматически обновится!

## Изменение контактов

Чтобы изменить контакты менеджеров (Telegram, WhatsApp), отредактируйте файлы:
- `src/components/Footer.tsx`
- `src/pages/Home.tsx`
- `src/pages/TourDetail.tsx`
- `src/pages/Contacts.tsx`

Найдите строки с `https://t.me/...` и `https://wa.me/...` и замените на свои контакты.

## Горячая перезагрузка

Vite поддерживает Hot Module Replacement (HMR):
- Изменения в файлах автоматически применяются без перезагрузки страницы
- Состояние компонентов сохраняется
- Очень удобно для разработки!

## Дополнительная информация

- **Документация Vite:** https://vitejs.dev/
- **Документация React:** https://react.dev/
- **Документация Tailwind CSS:** https://tailwindcss.com/

---

**Готово!** Теперь вы можете разрабатывать и тестировать сайт локально! 🚀
