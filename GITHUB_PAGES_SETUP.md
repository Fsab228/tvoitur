# Инструкция по размещению сайта на GitHub Pages

Эта инструкция поможет вам разместить ваш сайт с турами на GitHub Pages, чтобы люди могли заходить на него как на обычный сайт.

## Шаг 1: Установка Git

Если Git еще не установлен на вашем компьютере:

1. Скачайте Git с официального сайта: https://git-scm.com/download/win
2. Установите Git, следуя инструкциям установщика
3. Проверьте установку, открыв PowerShell и выполнив команду:
   ```powershell
   git --version
   ```

## Шаг 2: Создание аккаунта на GitHub

1. Перейдите на https://github.com
2. Нажмите "Sign up" (Регистрация)
3. Заполните форму регистрации
4. Подтвердите email

## Шаг 3: Создание репозитория на GitHub

1. Войдите в свой аккаунт GitHub
2. Нажмите кнопку "+" в правом верхнем углу и выберите "New repository"
3. Заполните форму:
   - **Repository name**: `tvoitur` (или любое другое имя)
   - **Description**: "Сайт туристического агентства"
   - Выберите **Public** (публичный репозиторий)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите "Create repository"

## Шаг 4: Настройка проекта локально

### 4.1. Инициализация Git репозитория

Откройте PowerShell в папке проекта и выполните команды:

```powershell
cd C:\Users\seraf\tvoitur
git init
```

### 4.2. Добавление всех файлов

```powershell
git add .
```

### 4.3. Создание первого коммита

```powershell
git commit -m "Первая версия сайта"
```

### 4.4. Подключение к удаленному репозиторию

**ВАЖНО:** Замените `ВАШ_USERNAME` на ваш реальный username на GitHub!

```powershell
git remote add origin https://github.com/ВАШ_USERNAME/tvoitur.git
```

Например, если ваш username `ivanov`, команда будет:
```powershell
git remote add origin https://github.com/ivanov/tvoitur.git
```

### 4.5. Переименование ветки в main

```powershell
git branch -M main
```

### 4.6. Отправка кода на GitHub

```powershell
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub. Используйте ваш username и **Personal Access Token** (см. Шаг 5).

## Шаг 5: Создание Personal Access Token

GitHub требует использовать токен вместо пароля:

1. Перейдите в GitHub → Settings (Настройки) → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Заполните форму:
   - **Note**: "Мой компьютер"
   - **Expiration**: выберите срок действия (например, 90 дней)
   - **Select scopes**: отметьте `repo` (все галочки в разделе repo)
4. Нажмите "Generate token"
5. **СОХРАНИТЕ ТОКЕН** - он показывается только один раз! Используйте его как пароль при `git push`

## Шаг 6: Включение GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (Настройки)
3. В левом меню найдите раздел **Pages**
4. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
5. Сохраните изменения

## Шаг 7: Обновление base path в vite.config.ts

**ВАЖНО:** Если имя вашего репозитория отличается от `tvoitur`, нужно обновить файл `vite.config.ts`:

1. Откройте файл `vite.config.ts`
2. Найдите строку с `base: '/tvoitur/'`
3. Замените `tvoitur` на имя вашего репозитория
4. Сохраните файл
5. Выполните команды:
   ```powershell
   git add vite.config.ts
   git commit -m "Обновлен base path для GitHub Pages"
   git push
   ```

## Шаг 8: Ожидание публикации

После выполнения `git push`:

1. GitHub Actions автоматически начнет сборку и публикацию сайта
2. Это займет 1-3 минуты
3. Проверьте статус: в репозитории перейдите на вкладку **Actions**
4. Когда сборка завершится успешно (зеленая галочка), сайт будет доступен

## Шаг 9: Доступ к сайту

Ваш сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/tvoitur/
```

Например, если ваш username `ivanov`:
```
https://ivanov.github.io/tvoitur/
```

## Обновление сайта

Когда вы хотите обновить сайт:

1. Внесите изменения в файлы проекта
2. Выполните команды:
   ```powershell
   cd C:\Users\seraf\tvoitur
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Подождите 1-3 минуты - сайт автоматически обновится!

## Использование собственного домена (опционально)

Если у вас есть свой домен (например, `moi-tury.ru`):

1. В настройках репозитория → Pages добавьте ваш домен в поле "Custom domain"
2. Настройте DNS записи у вашего регистратора домена:
   - Тип: `CNAME`
   - Имя: `@` или `www`
   - Значение: `ВАШ_USERNAME.github.io`
3. Подождите распространения DNS (может занять до 24 часов)

## Решение проблем

### Сайт не открывается
- Проверьте, что сборка завершилась успешно (вкладка Actions)
- Убедитесь, что в Settings → Pages выбран источник "GitHub Actions"
- Проверьте правильность base path в `vite.config.ts`

### Ошибки при git push
- Убедитесь, что используете Personal Access Token, а не пароль
- Проверьте правильность URL репозитория

### Изображения не загружаются
- Убедитесь, что все изображения находятся в папке `public/images/`
- Проверьте пути к изображениям в коде

## Полезные команды Git

```powershell
# Проверить статус изменений
git status

# Посмотреть историю коммитов
git log

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1
```

---

**Готово!** Ваш сайт теперь доступен в интернете! 🎉
