# 📖 Полная инструкция по публикации сайта tvoitur на GitHub Pages

## 📋 Содержание

1. [Подготовка](#1-подготовка)
2. [Создание репозитория на GitHub](#2-создание-репозитория-на-github)
3. [Настройка проекта локально](#3-настройка-проекта-локально)
4. [Отправка кода на GitHub](#4-отправка-кода-на-github)
5. [Включение GitHub Pages](#5-включение-github-pages)
6. [Проверка публикации](#6-проверка-публикации)
7. [Решение проблем](#7-решение-проблем)
8. [Обновление сайта](#8-обновление-сайта)

---

## 1. Подготовка

### 1.1. Установка Git

**Если Git уже установлен, пропустите этот шаг.**

1. Откройте браузер и перейдите на: https://git-scm.com/download/win
2. Скачайте установщик Git для Windows
3. Запустите установщик
4. Во время установки:
   - Оставьте все настройки по умолчанию
   - Нажмите "Next" на каждом шаге
   - Нажмите "Install" для начала установки
5. После установки нажмите "Finish"

**Проверка установки:**
1. Откройте PowerShell (нажмите `Win + X` и выберите "Windows PowerShell" или "Терминал")
2. Введите команду:
   ```powershell
   git --version
   ```
3. Должно появиться что-то вроде: `git version 2.xx.x`

✅ **Если видите версию Git — всё готово!**

---

### 1.2. Создание аккаунта на GitHub

1. Откройте браузер и перейдите на: https://github.com
2. Нажмите кнопку **"Sign up"** (Регистрация) в правом верхнем углу
3. Заполните форму регистрации:
   - **Username** (имя пользователя) — выберите уникальное имя (например: `ivanov`, `maria-tours`)
   - **Email** — введите ваш email
   - **Password** — придумайте надежный пароль
4. Нажмите **"Create account"**
5. Подтвердите email:
   - Проверьте почту
   - Откройте письмо от GitHub
   - Нажмите на ссылку подтверждения

✅ **Аккаунт создан! Запомните ваш username — он понадобится дальше.**

---

## 2. Создание репозитория на GitHub

### 2.1. Создание нового репозитория

1. Войдите в свой аккаунт GitHub (https://github.com)
2. В правом верхнем углу нажмите кнопку **"+"** (плюс)
3. В выпадающем меню выберите **"New repository"**

### 2.2. Заполнение формы создания репозитория

**ВАЖНО:** Заполните форму точно так, как указано ниже!

1. **Repository name** (Имя репозитория):
   - Введите: `tvoitur`
   - ⚠️ Имя должно быть точно `tvoitur` (строчными буквами)

2. **Description** (Описание):
   - Введите: `Сайт туристического агентства ТвойТур`

3. **Visibility** (Видимость):
   - Выберите: **Public** (публичный)
   - ⚠️ Репозиторий должен быть публичным для бесплатного GitHub Pages

4. **НЕ СТАВЬТЕ ГАЛОЧКИ** на:
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license

5. Нажмите кнопку **"Create repository"** (зеленая кнопка внизу)

✅ **Репозиторий создан!**

После создания вы увидите страницу с инструкциями. **НЕ СЛЕДУЙТЕ** этим инструкциям — мы сделаем всё по-другому.

---

## 3. Настройка проекта локально

### 3.1. Открытие PowerShell в папке проекта

**Способ 1 (через Проводник):**
1. Откройте Проводник Windows
2. Перейдите в папку: `C:\Users\seraf\tvoitur`
3. В адресной строке введите: `powershell` и нажмите Enter
4. Откроется PowerShell в нужной папке

**Способ 2 (через PowerShell):**
1. Откройте PowerShell
2. Введите команду:
   ```powershell
   cd C:\Users\seraf\tvoitur
   ```

### 3.2. Инициализация Git репозитория

В PowerShell выполните команду:

```powershell
git init
```

**Что произошло:**
- Git создал скрытую папку `.git` в вашей папке проекта
- Теперь Git отслеживает изменения в файлах

**Ожидаемый результат:**
```
Initialized empty Git repository in C:/Users/seraf/tvoitur/.git/
```

### 3.3. Добавление всех файлов в Git

Выполните команду:

```powershell
git add .
```

**Что произошло:**
- Все файлы проекта добавлены в "staging area" (область подготовки)
- Git готов зафиксировать эти файлы

**Ожидаемый результат:**
- Команда выполнится без ошибок (может быть предупреждение о переводе строк, это нормально)

### 3.4. Создание первого коммита

Выполните команду:

```powershell
git commit -m "Первая версия сайта tvoitur"
```

**Что произошло:**
- Git создал "снимок" всех файлов проекта
- Этот снимок называется "коммит"
- Сообщение коммита: "Первая версия сайта tvoitur"

**Ожидаемый результат:**
```
[main (root-commit) xxxxxxx] Первая версия сайта tvoitur
 44 files changed, 7834 insertions(+)
```

### 3.5. Переименование ветки в main

Выполните команду:

```powershell
git branch -M main
```

**Что произошло:**
- Ветка переименована в `main` (стандартное имя для GitHub)

**Ожидаемый результат:**
- Команда выполнится без вывода (это нормально)

### 3.6. Подключение к удаленному репозиторию

**ВАЖНО:** Замените `ВАШ_USERNAME` на ваш реальный username на GitHub!

Выполните команду (замените `ВАШ_USERNAME`):

```powershell
git remote add origin https://github.com/ВАШ_USERNAME/tvoitur.git
```

**Пример:**
Если ваш username `ivanov`, команда будет:
```powershell
git remote add origin https://github.com/ivanov/tvoitur.git
```

**Что произошло:**
- Git теперь знает, куда отправлять код (на GitHub)

**Ожидаемый результат:**
- Команда выполнится без вывода (это нормально)

**Если увидите ошибку "remote origin already exists":**
- Это значит, что репозиторий уже подключен
- Выполните команду:
  ```powershell
  git remote set-url origin https://github.com/ВАШ_USERNAME/tvoitur.git
  ```

---

## 4. Отправка кода на GitHub

### 4.1. Создание Personal Access Token

GitHub требует использовать токен вместо пароля для безопасности.

#### Шаг 1: Переход в настройки токенов

1. Откройте GitHub в браузере: https://github.com
2. В правом верхнем углу нажмите на ваш аватар
3. В меню выберите **"Settings"** (Настройки)

#### Шаг 2: Открытие раздела токенов

1. В левом меню прокрутите вниз
2. Найдите раздел **"Developer settings"** (Настройки разработчика)
3. Нажмите на него
4. В левом меню выберите **"Personal access tokens"**
5. Выберите **"Tokens (classic)"**

#### Шаг 3: Создание нового токена

1. Нажмите кнопку **"Generate new token"**
2. Выберите **"Generate new token (classic)"**

#### Шаг 4: Настройка токена

1. **Note** (Название):
   - Введите: `Мой компьютер` или `tvoitur`

2. **Expiration** (Срок действия):
   - Выберите: `90 days` (90 дней) или `No expiration` (без срока)

3. **Select scopes** (Выбор прав):
   - Найдите раздел **"repo"**
   - Поставьте галочку на **"repo"** (это автоматически выберет все подпункты)
   - Должны быть выбраны:
     - ✅ repo (все галочки)
     - ✅ workflow (если видите)

4. Прокрутите вниз и нажмите **"Generate token"** (зеленая кнопка)

#### Шаг 5: Сохранение токена

⚠️ **КРИТИЧЕСКИ ВАЖНО:**

- GitHub покажет токен **ТОЛЬКО ОДИН РАЗ**
- Скопируйте токен сразу (он выглядит как: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
- Сохраните токен в безопасном месте (блокнот, менеджер паролей)
- Если потеряете токен — придется создать новый

✅ **Токен создан и сохранен!**

### 4.2. Отправка кода на GitHub

Вернитесь в PowerShell и выполните команду:

```powershell
git push -u origin main
```

**Что произойдет:**
1. Git попросит ввести **Username** (имя пользователя GitHub)
   - Введите ваш username (например: `ivanov`)
   - Нажмите Enter

2. Git попросит ввести **Password** (пароль)
   - ⚠️ **НЕ ВВОДИТЕ** ваш обычный пароль GitHub!
   - Введите **Personal Access Token**, который вы только что создали
   - Нажмите Enter

**Ожидаемый результат:**
```
Enumerating objects: 44, done.
Counting objects: 100% (44/44), done.
Delta compression using up to 8 threads
Compressing objects: 100% (42/42), done.
Writing objects: 100% (44/44), done.
To https://github.com/ВАШ_USERNAME/tvoitur.git
 * [new branch]      main -> main
Branch 'main' set up to track 'remote/origin/main'.
```

✅ **Код успешно отправлен на GitHub!**

**Если возникла ошибка:**
- См. раздел [Решение проблем](#7-решение-проблем)

---

## 5. Включение GitHub Pages

### 5.1. Переход в настройки репозитория

1. Откройте ваш репозиторий на GitHub: https://github.com/ВАШ_USERNAME/tvoitur
2. Нажмите на вкладку **"Settings"** (Настройки) в верхнем меню

### 5.2. Открытие раздела Pages

1. В левом меню найдите раздел **"Pages"**
2. Нажмите на него

### 5.3. Настройка источника

1. Найдите раздел **"Source"** (Источник)
2. В выпадающем списке выберите: **"GitHub Actions"**
3. ⚠️ **НЕ ВЫБИРАЙТЕ** "Deploy from a branch"!

**Ожидаемый результат:**
- После выбора "GitHub Actions" появится сообщение о том, что можно использовать workflows

✅ **GitHub Pages настроен!**

---

## 6. Проверка публикации

### 6.1. Проверка статуса сборки

1. В репозитории на GitHub перейдите на вкладку **"Actions"**
2. Вы должны увидеть запущенный workflow **"Deploy to GitHub Pages"**
3. Нажмите на него, чтобы увидеть детали

**Что происходит:**
- GitHub автоматически запускает сборку вашего сайта
- Это занимает **1-3 минуты**

**Статусы:**
- 🟡 **Желтый кружок** — сборка в процессе (подождите)
- ✅ **Зеленая галочка** — сборка успешна, сайт опубликован
- ❌ **Красный крестик** — ошибка сборки (см. раздел [Решение проблем](#7-решение-проблем))

### 6.2. Открытие сайта

После успешной сборки (зеленая галочка):

1. Вернитесь в **Settings → Pages**
2. Вверху страницы вы увидите сообщение:
   ```
   Your site is live at https://ВАШ_USERNAME.github.io/tvoitur/
   ```

3. Нажмите на ссылку или скопируйте её в браузер

**Пример URL:**
```
https://ivanov.github.io/tvoitur/
```

### 6.3. Проверка содержимого сайта

На главной странице вы должны увидеть:

✅ **Главная страница (`/`):**
- Заголовок "Откройте мир путешествий с ТвойТур"
- Секция "Горящие туры" с карточками туров
- Секция "Популярные туры" с карточками туров
- Секция "Акции и спецпредложения"

✅ **Страница всех туров (`/tours`):**
- Все 24 тура в каталоге
- Фильтры по категориям, цене, стране
- Поиск туров
- Сортировка

✅ **Страница горящих туров (`/hot-tours`):**
- Туры со скидкой более 20%

✅ **Навигация:**
- Меню в шапке сайта
- Все ссылки работают

✅ **Отзывы:**
- На главной странице в секции отзывов (если добавлены)

---

## 7. Решение проблем

### Проблема 1: "git: команда не найдена"

**Причина:** Git не установлен или не добавлен в PATH

**Решение:**
1. Переустановите Git: https://git-scm.com/download/win
2. При установке убедитесь, что выбрана опция "Add Git to PATH"
3. Перезапустите PowerShell

---

### Проблема 2: "remote origin already exists"

**Причина:** Репозиторий уже подключен

**Решение:**
```powershell
git remote remove origin
git remote add origin https://github.com/ВАШ_USERNAME/tvoitur.git
```

---

### Проблема 3: "Authentication failed" при git push

**Причина:** Неправильный токен или пароль

**Решение:**
1. Убедитесь, что используете **Personal Access Token**, а не пароль
2. Проверьте, что токен не истек
3. Создайте новый токен, если нужно
4. Попробуйте снова:
   ```powershell
   git push -u origin main
   ```

---

### Проблема 4: "Updates were rejected" при git push

**Причина:** В удаленном репозитории есть изменения, которых нет локально

**Решение:**

**Вариант 1 (если в удаленном репозитории нет важных данных):**
```powershell
git push -u origin main --force
```
⚠️ Используйте осторожно — это перезапишет удаленный репозиторий!

**Вариант 2 (если нужно сохранить удаленные изменения):**
```powershell
git pull origin main --allow-unrelated-histories
# Разрешите конфликты, если они возникнут
git add .
git commit -m "Merge remote changes"
git push -u origin main
```

---

### Проблема 5: Сайт не открывается или показывает ошибку 404

**Причина:** Неправильный base path или GitHub Pages не настроен

**Решение:**

1. **Проверьте настройки GitHub Pages:**
   - Settings → Pages → Source должен быть **"GitHub Actions"**

2. **Проверьте base path в vite.config.ts:**
   - Откройте файл `C:\Users\seraf\tvoitur\vite.config.ts`
   - Убедитесь, что там написано:
     ```typescript
     base: process.env.NODE_ENV === 'production' ? '/tvoitur/' : '/',
     ```
   - Если имя репозитория другое, замените `/tvoitur/` на `/ваше-имя-репозитория/`

3. **Проверьте сборку:**
   - Actions → найдите последний workflow
   - Если красный крестик — нажмите на него и посмотрите ошибку

4. **Очистите кэш браузера:**
   - Нажмите `Ctrl + Shift + Delete`
   - Выберите "Кэшированные изображения и файлы"
   - Нажмите "Удалить данные"
   - Или откройте сайт в режиме инкогнито (`Ctrl + Shift + N`)

---

### Проблема 6: Туры не отображаются на сайте

**Причина:** Неправильная настройка React Router или старая версия сайта

**Решение:**

1. **Проверьте, что используете правильный URL:**
   - Главная: `https://ВАШ_USERNAME.github.io/tvoitur/`
   - Туры: `https://ВАШ_USERNAME.github.io/tvoitur/tours`

2. **Проверьте файл `src/App.tsx`:**
   - Убедитесь, что там есть строка:
     ```typescript
     const basename = import.meta.env.BASE_URL || '/'
     ```
   - И в Router указано:
     ```typescript
     <Router basename={basename}>
     ```

3. **Пересоберите и отправьте заново:**
   ```powershell
   git add .
   git commit -m "Fix routing"
   git push
   ```
   - Подождите 1-3 минуты, пока GitHub Actions соберет сайт заново

---

### Проблема 7: Ошибка сборки в GitHub Actions

**Причина:** Ошибка в коде или конфигурации

**Решение:**

1. Откройте вкладку **Actions** в репозитории
2. Нажмите на упавший workflow (красный крестик)
3. Нажмите на задачу "Build" (с красным крестиком)
4. Прокрутите вниз до раздела "Run npm run build"
5. Посмотрите на ошибку

**Частые ошибки:**

- **"Cannot find module"** — не все зависимости установлены
  - Проверьте `package.json`
  - Убедитесь, что все файлы закоммичены

- **"Syntax error"** — ошибка в коде
  - Проверьте файлы в папке `src/`
  - Исправьте ошибку и отправьте снова

---

### Проблема 8: Изображения не загружаются

**Причина:** Неправильные пути к изображениям

**Решение:**

1. Убедитесь, что изображения находятся в папке `public/images/`
2. В коде используйте пути относительно `public/`:
   - Правильно: `/images/tour1.jpg`
   - Неправильно: `./images/tour1.jpg` или `images/tour1.jpg`

---

## 8. Обновление сайта

### 8.1. Внесение изменений

1. Откройте файлы проекта в редакторе (VS Code, Notepad++ и т.д.)
2. Внесите нужные изменения
3. Сохраните файлы (`Ctrl + S`)

### 8.2. Отправка изменений на GitHub

В PowerShell выполните команды:

```powershell
cd C:\Users\seraf\tvoitur
git add .
git commit -m "Описание ваших изменений"
git push
```

**Примеры сообщений коммита:**
- `"Обновлены цены на туры"`
- `"Добавлены новые туры"`
- `"Исправлена ошибка в контактах"`
- `"Обновлен дизайн главной страницы"`

### 8.3. Ожидание обновления

1. GitHub Actions автоматически запустит сборку
2. Подождите **1-3 минуты**
3. Обновите страницу сайта в браузере (`F5` или `Ctrl + R`)

✅ **Сайт обновлен!**

---

## 📝 Полезные команды Git

### Проверка статуса

```powershell
git status
```

Показывает, какие файлы изменены, но не закоммичены.

---

### Просмотр истории коммитов

```powershell
git log
```

Показывает все коммиты. Для выхода нажмите `q`.

---

### Отмена последнего коммита (сохранить изменения)

```powershell
git reset --soft HEAD~1
```

Отменяет последний коммит, но сохраняет все изменения в файлах.

---

### Просмотр изменений в файлах

```powershell
git diff
```

Показывает, что именно изменилось в файлах.

---

### Отмена всех незакоммиченных изменений

```powershell
git checkout .
```

⚠️ **Осторожно!** Это удалит все незакоммиченные изменения!

---

## 🎯 Чек-лист перед публикацией

Перед тем как публиковать сайт, убедитесь:

- [ ] Git установлен и работает (`git --version`)
- [ ] Аккаунт на GitHub создан
- [ ] Репозиторий `tvoitur` создан на GitHub (публичный)
- [ ] Personal Access Token создан и сохранен
- [ ] Проект инициализирован (`git init`)
- [ ] Все файлы добавлены (`git add .`)
- [ ] Создан первый коммит (`git commit`)
- [ ] Репозиторий подключен (`git remote add origin`)
- [ ] Код отправлен на GitHub (`git push`)
- [ ] GitHub Pages включен (Settings → Pages → GitHub Actions)
- [ ] Сборка завершилась успешно (зеленая галочка в Actions)
- [ ] Сайт открывается по адресу `https://ВАШ_USERNAME.github.io/tvoitur/`
- [ ] Все туры отображаются на странице `/tours`
- [ ] Навигация работает корректно

---

## 📞 Дополнительная помощь

### Полезные ссылки

- **Документация Git:** https://git-scm.com/doc
- **Документация GitHub Pages:** https://docs.github.com/en/pages
- **Документация Vite:** https://vitejs.dev/
- **Документация React Router:** https://reactrouter.com/

### Часто задаваемые вопросы

**Q: Можно ли изменить имя репозитория?**
A: Да, но после изменения нужно обновить `base` в `vite.config.ts` и пересобрать сайт.

**Q: Сколько времени занимает публикация?**
A: Обычно 1-3 минуты после `git push`.

**Q: Можно ли использовать свой домен?**
A: Да, в Settings → Pages можно добавить Custom domain.

**Q: Как удалить сайт с GitHub Pages?**
A: В Settings → Pages выберите "None" в Source и сохраните.

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:

```
https://ВАШ_USERNAME.github.io/tvoitur/
```

**Поздравляем! Ваш сайт с турами теперь в интернете! 🎉**

---

*Последнее обновление инструкции: 2024*
